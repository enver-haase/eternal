::sysinit:/bin/busybox echo =========================================
::sysinit:/bin/busybox echo Starting fbdoom on MMU-protected subleq Linux
::sysinit:/bin/busybox echo =========================================
::sysinit:/bin/busybox mount -t proc proc /proc
# Launch fbdoom on the console (stdin=keyboard, renders to /dev/fb0). The attract-mode
# demo runs without input. respawn so it restarts if it exits.
console::respawn:/doom
